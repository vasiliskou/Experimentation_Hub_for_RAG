# Mandatory: OpenAI API key (needed for gpt-4o, gpt-4o-mini, gpt-5-mini, etc.)
OPENAI_API_KEY=

# Optional: Anthropic API key (for Claude models)
ANTHROPIC_API_KEY=

# Optional: Google API key (for Gemini models)
GOOGLE_API_KEY=

# Optional: DeepSeek API key (for DeepSeek models)
DEEPSEEK_API_KEY=

# Optional: Groq API key (for LLaMA models served by Groq)
GROQ_API_KEY=

# Optional: HuggingFace token (for embeddings)
HF_TOKEN=

# Optional: Serper API key (used for web search in Online RAG & Agentic RAG)
SERPER_API_KEY=

# Optional: Cohere API key (for Cohere embeddings)
COHERE_API_KEY=

# Optional: Pinecone API key (for Pinecone cloud vector database)
PINECONE_API_KEY=
